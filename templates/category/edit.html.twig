{% extends 'base.html.twig' %}

{% block title %}Modifier la catégorie{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center mb-4">Modifier la catégorie</h1>

    <div class="card mx-auto shadow" style="max-width: 500px;">
        <div class="card-body">
            {{ include('category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
        <div class="card-footer d-flex justify-content-between flex-wrap">
            <a href="{{ path('app_category_index') }}" class="btn btn-outline-secondary">← Retour</a>
            {{ include('category/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
